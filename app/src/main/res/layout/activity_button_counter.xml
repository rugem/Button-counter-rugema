<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_button_counter"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="edu.cofc.csci490.button_counter_rugema.ButtonCounterActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/text_title"
        android:id="@+id/counterTextView"
        android:textSize="50sp"
        android:textStyle="bold"
        android:textColor="@color/blue_text_color"
        android:layout_marginTop="47dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="@string/up_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/count_up_button"
        android:textSize="50sp"
        android:layout_marginTop="@dimen/top"
        android:textStyle="bold"
        android:layout_below="@+id/counterTextView"
        android:layout_toLeftOf="@+id/counterTextView"
        android:layout_toStartOf="@+id/counterTextView" />

    <Button
        android:text="@string/down_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/count_down_button"
        android:textSize="50sp"
        android:textStyle="bold"
        android:layout_marginTop="@dimen/top"
        android:layout_below="@+id/counterTextView"
        android:layout_toRightOf="@+id/counterTextView"
        android:layout_toEndOf="@+id/counterTextView" />

    <Button
        android:text="@string/clear_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/clear_button"
        android:textSize="30sp"
        android:textStyle="bold"
        android:height="80dp"
        android:layout_marginTop="@dimen/top"
        android:layout_below="@+id/count_up_button"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
